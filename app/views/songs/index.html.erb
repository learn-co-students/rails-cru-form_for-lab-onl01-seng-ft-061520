<h1>Song#index</h1>
<p>Find me in app/views/song/index.html.erb</p>

<% @songs.each do |song| %>

<p><%= link_to song.name, song_path(song) %></p>
<p><%= link_to 'genre', genre_path(song) %></p>
<p><%= link_to 'artist', artist_path(song) %></p>
<% end %>